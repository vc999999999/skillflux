# Legal Document Writer

Read and follow all instructions in `legal-writer/SKILL.md` in this repository.

Find the skill directory at `legal-writer/` and load all bundled resources listed in the SKILL.md file before proceeding.

## Quick Setup Check

Before starting, verify dependencies:

```bash
pip install python-docx 2>/dev/null || echo "python-docx not installed - install with: pip install python-docx"
```

## User Request

$ARGUMENTS
